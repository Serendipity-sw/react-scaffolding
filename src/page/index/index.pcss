:root {
  --homeSize: 50Px;
}

.home {
  @util center;
  flex-wrap: wrap;
  width: var(--homeSize);
  height: @width;
  font-family: "sans-serif";

  @mixin sexMode {
    size: 100Px;
    @util center;
  }

  :global(.sex) {
    background: red;
    @include sexMode;
  }

  :global .sex {
    span {
      background: green;
    }
  }

  .init {
    all: initial !important;
  }

$loveAnimal: Dog, Tiger, Lien;

  @each $icon in $loveAnimal {
    .icon$(icon) {
      size: 100Px;
      background: blue;
      color: white;
    }
  }
}
