.init {
  width: 100%;
  height: @width;
  @util center;
  flex-direction: column;

  .img {
    display: block;
    background: url('./images/title-left-icon.png') no-repeat center;
    background-size: 100% 100%;
    width: 50px;
    height: @width;
  }
}
